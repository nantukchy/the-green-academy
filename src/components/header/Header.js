import React from 'react';
import MenuBar from '../menu/MenuBar';

import './Header.css';

const Header = () => {
  
    return (
        <div>
            <MenuBar/>
        </div>
    );
};

export default Header;